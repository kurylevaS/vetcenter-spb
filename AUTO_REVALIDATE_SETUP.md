# Автоматическая ревалидация страниц (без webhook'ов)

## 🎯 Простое решение без настройки WordPress

Этот метод автоматически проверяет WordPress на наличие изменений и обновляет страницы Next.js **без необходимости настройки webhook'ов в WordPress**.

## Как это работает

1. **Автоматический polling** — Next.js периодически проверяет WordPress API на наличие измененных постов
2. **Автоматическая ревалидация** — при обнаружении изменений страницы автоматически обновляются
3. **Никаких настроек в WordPress** — всё работает только на стороне Next.js

## Быстрая настройка

### Шаг 1: Добавьте секретный ключ

Сгенерируйте и добавьте в `.env.local`:

```bash
openssl rand -base64 32
```

```env
REVALIDATE_SECRET=ваш-секретный-ключ-здесь
```

### Шаг 2: Настройка автоматической проверки через Cron

#### Настройка Cron Job на VPS

**Рекомендуемый способ (через Authorization header):**

1. Откройте crontab для редактирования:
   ```bash
   crontab -e
   ```

2. Добавьте строку (замените `ваш-секретный-ключ` и `your-domain.com`):
   ```bash
   # Автоматическая проверка изменений в WordPress каждые 5 минут
   */5 * * * * curl -s -H "Authorization: Bearer ваш-секретный-ключ" https://your-domain.com/api/revalidate/check > /dev/null 2>&1
   ```

3. Сохраните и закройте редактор (в nano: `Ctrl+X`, затем `Y`, затем `Enter`)

4. Проверьте, что cron job добавлен:
   ```bash
   crontab -l
   ```

**Альтернативный способ (через query параметр):**

Если предпочитаете использовать query параметр:

```bash
*/5 * * * * curl -s "https://your-domain.com/api/revalidate/check?secret=ваш-секретный-ключ" > /dev/null 2>&1
```

⚠️ **Важно:** Использование Authorization header более безопасно, так как секретный ключ не попадает в логи.

#### Альтернатива: Внешний сервис (cron-job.org, EasyCron и т.д.)

Если у вас нет доступа к cron на сервере, можно использовать внешний сервис:

1. Зарегистрируйтесь на [cron-job.org](https://cron-job.org) или аналогичном сервисе
2. Создайте новую задачу:
   - **URL**: `https://your-domain.com/api/revalidate/check?secret=ваш-секретный-ключ`
   - **Schedule**: каждые 5 минут
   - **Method**: GET

#### Ручной вызов (для тестирования)

```bash
curl "https://your-domain.com/api/revalidate/check?secret=ваш-секретный-ключ"
```

Или через Authorization header:

```bash
curl -H "Authorization: Bearer ваш-секретный-ключ" https://your-domain.com/api/revalidate/check
```

## Что проверяется автоматически

Система автоматически проверяет и ревалидирует следующие типы контента:

- ✅ **Акции (Promo)** → `/promos/{slug}`, `/promos`, `/`
- ✅ **Услуги (Service)** → `/services/{slug}`, `/services`, `/`
- ✅ **Врачи (Doctor)** → `/doctors/{id}`, `/doctors`, `/`
- ✅ **Статьи блога (Blog Post)** → `/blog/{slug}`, `/blog`, `/`
- ✅ **Страницы (Page)** → `/{slug}` или `/` для главной
- ✅ **Типы услуг (Service Types)** → `/service-types/{slug}`, `/service-types`

## Настройка частоты проверки

По умолчанию проверка происходит каждые 5 минут. Вы можете изменить это в crontab:

Измените расписание в crontab (`crontab -e`):

```bash
# Каждую минуту
* * * * * curl -s -H "Authorization: Bearer ваш-ключ" https://your-domain.com/api/revalidate/check > /dev/null 2>&1

# Каждые 2 минуты
*/2 * * * * curl -s -H "Authorization: Bearer ваш-ключ" https://your-domain.com/api/revalidate/check > /dev/null 2>&1

# Каждые 5 минут (рекомендуется)
*/5 * * * * curl -s -H "Authorization: Bearer ваш-ключ" https://your-domain.com/api/revalidate/check > /dev/null 2>&1

# Каждые 10 минут
*/10 * * * * curl -s -H "Authorization: Bearer ваш-ключ" https://your-domain.com/api/revalidate/check > /dev/null 2>&1

# Каждый час
0 * * * * curl -s -H "Authorization: Bearer ваш-ключ" https://your-domain.com/api/revalidate/check > /dev/null 2>&1
```

**Формат cron:** `минута час день месяц день_недели`

- `*/1 * * * *` — каждую минуту
- `*/5 * * * *` — каждые 5 минут (рекомендуется)
- `*/10 * * * *` — каждые 10 минут
- `0 * * * *` — каждый час в начале часа

## Проверка работы

### 1. Проверьте, что endpoint доступен

```bash
curl https://your-domain.com/api/revalidate/check?secret=ваш-ключ
```

Должен вернуться JSON с результатами проверки:

```json
{
  "success": true,
  "checkedSince": "2024-01-01T12:00:00.000Z",
  "checkedUntil": "2024-01-01T12:05:00.000Z",
  "revalidatedCount": 3,
  "revalidatedPaths": [
    "/promos/summer-sale",
    "/promos",
    "/"
  ],
  "timestamp": 1704115200000
}
```

### 2. Внесите изменение в WordPress

1. Откройте любой пост в WordPress
2. Внесите небольшое изменение
3. Сохраните

### 3. Дождитесь следующей проверки

Подождите до следующего запуска cron (максимум 5 минут) или запустите вручную:

```bash
curl "https://your-domain.com/api/revalidate/check?secret=ваш-ключ"
```

### 4. Проверьте результат

- Проверьте логи Next.js приложения
- Проверьте, что изменения отобразились на сайте (может потребоваться очистка кеша браузера)

## Параметры запроса

### Query параметры

- `secret` (обязательный) — секретный ключ из `REVALIDATE_SECRET`
- `since` (опциональный) — ISO дата для проверки изменений с определенного времени
  - Пример: `?secret=xxx&since=2024-01-01T12:00:00Z`

### Authorization Header

Вместо query параметра можно использовать заголовок:

```bash
curl -H "Authorization: Bearer ваш-секретный-ключ" https://your-domain.com/api/revalidate/check
```

## Логирование

Все действия логируются в консоль Next.js:

```
Проверка изменений с 2024-01-01T12:00:00.000Z
Найдено 2 измененных постов типа promo
Ревалидирован путь: /promos/summer-sale
Ревалидирован путь: /promos
Ревалидирован путь: /
```

## Преимущества этого метода

✅ **Простота** — не нужно настраивать ничего в WordPress  
✅ **Надежность** — работает даже если WordPress временно недоступен  
✅ **Гибкость** — легко настроить частоту проверки  
✅ **Безопасность** — защищено секретным ключом  
✅ **Автоматизация** — работает полностью автоматически  

## Недостатки

⚠️ **Задержка** — изменения появляются не мгновенно, а в течение интервала проверки (по умолчанию до 5 минут)  
⚠️ **Нагрузка** — периодические запросы к WordPress API  

## Troubleshooting

### Endpoint возвращает 401

- Проверьте, что `REVALIDATE_SECRET` установлен в переменных окружения
- Убедитесь, что секретный ключ в запросе совпадает с `REVALIDATE_SECRET`
- Перезапустите Next.js приложение после изменения переменных окружения

### Изменения не появляются

1. Проверьте логи Next.js — должны быть сообщения о ревалидации
2. Убедитесь, что cron job настроен и работает
3. Проверьте, что WordPress API доступен и возвращает данные
4. Попробуйте запустить проверку вручную

### Ошибка подключения к WordPress API

- Проверьте, что `NEXT_PUBLIC_FRONT_API_URL` установлен правильно
- Убедитесь, что WordPress API доступен из сервера Next.js
- Проверьте логи для деталей ошибки

## Настройка на VPS (детальная инструкция)

### 1. Подключитесь к серверу

```bash
ssh root@your-server-ip
# или
ssh your-user@your-server-ip
```

### 2. Проверьте, что cron установлен

```bash
which cron
# или
which crond
```

Если cron не установлен (для Ubuntu/Debian):

```bash
sudo apt update
sudo apt install cron
sudo systemctl enable cron
sudo systemctl start cron
```

### 3. Создайте скрипт для удобства (опционально)

Создайте файл `/usr/local/bin/revalidate-check.sh`:

```bash
sudo nano /usr/local/bin/revalidate-check.sh
```

Добавьте содержимое:

```bash
#!/bin/bash
# Скрипт для автоматической проверки изменений в WordPress

SECRET="ваш-секретный-ключ-здесь"
DOMAIN="https://your-domain.com"

curl -s -H "Authorization: Bearer $SECRET" "$DOMAIN/api/revalidate/check" > /dev/null 2>&1
```

Сделайте скрипт исполняемым:

```bash
sudo chmod +x /usr/local/bin/revalidate-check.sh
```

Теперь в crontab можно использовать:

```bash
*/5 * * * * /usr/local/bin/revalidate-check.sh
```

### 4. Проверка работы cron

Проверьте логи cron:

```bash
# Ubuntu/Debian
sudo tail -f /var/log/syslog | grep CRON

# CentOS/RHEL
sudo tail -f /var/log/cron
```

Или проверьте логи вашего приложения Next.js:

```bash
# Если используете PM2
pm2 logs vetcenter-spb

# Или проверьте логи напрямую
tail -f /var/www/vetcenter-spb/logs/combined.log
```

## Рекомендации

- 🕐 **Частота проверки**: 5 минут — оптимальный баланс между актуальностью и нагрузкой
- 🔒 **Безопасность**: Используйте Authorization header вместо query параметра для безопасности
- 📊 **Мониторинг**: Следите за логами для выявления проблем
- ⚡ **Производительность**: При большом количестве изменений рассмотрите увеличение интервала проверки
- 🛡️ **Защита**: Ограничьте доступ к endpoint через firewall или nginx, если возможно

## Сравнение методов

| Метод | Задержка | Сложность настройки | Нагрузка |
|-------|----------|-------------------|----------|
| **Автоматический polling** (этот метод) | До 5 минут | ⭐ Простая | Низкая |
| Webhook из WordPress | Мгновенно | ⭐⭐⭐ Сложная | Минимальная |
| ISR по времени | До 60 секунд | ⭐ Очень простая | Средняя |

Текущий проект использует комбинацию: **ISR (60 сек) + Автоматический polling (5 мин)** для оптимального баланса.

